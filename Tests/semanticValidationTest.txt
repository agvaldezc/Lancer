program agvaldezc;

var x : float; z : float; y : int; w : float; t : string;

func int funcion1(int a, int b, int c, string d) {
    a = b + c;
}

main {
    x = (z * (y - w));
    y = input;
    w = x + y / z;
    x = x + 423;
}

